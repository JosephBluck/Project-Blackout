#include "MathFunctions.h"

float SinDeg(float degrees) { return sin(degrees * (180.0f / PI)); } //Sine function using degrees instead of radians
float CosDeg(float degrees) { return cos(degrees * (180.0f / PI)); } //Cosine function using degrees instead of radians